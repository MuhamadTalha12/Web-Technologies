# LocalConnect Server (Express + MongoDB)

## Setup

1) Create `.env` from `.env.example`

2) Install dependencies:

- `cd server`
- `npm install`

3) Run:

- `npm run dev`

API base URL: `http://localhost:5000/api`

Uploads served at: `http://localhost:5000/uploads/...`
